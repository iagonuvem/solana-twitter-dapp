/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { TWITTER_PROGRAM_ADDRESS } from '../programs';
import { expectAddress, getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const COMMENT_TWEET_DISCRIMINATOR = new Uint8Array([
  26, 45, 20, 239, 43, 253, 168, 37,
]);

const seed = new TextEncoder().encode("COMMENT_SEED");

export function getCommentTweetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMMENT_TWEET_DISCRIMINATOR
  );
}

export type CommentTweetInstruction<
  TProgram extends string = typeof TWITTER_PROGRAM_ADDRESS,
  TAccountCommentAuthor extends string | AccountMeta<string> = string,
  TAccountComment extends string | AccountMeta<string> = string,
  TAccountTweet extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCommentAuthor extends string
        ? WritableSignerAccount<TAccountCommentAuthor> &
            AccountSignerMeta<TAccountCommentAuthor>
        : TAccountCommentAuthor,
      TAccountComment extends string
        ? WritableAccount<TAccountComment>
        : TAccountComment,
      TAccountTweet extends string
        ? WritableAccount<TAccountTweet>
        : TAccountTweet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CommentTweetInstructionData = {
  discriminator: ReadonlyUint8Array;
  commentContent: string;
};

export type CommentTweetInstructionDataArgs = { commentContent: string };

export function getCommentTweetInstructionDataEncoder(): Encoder<CommentTweetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      [
        'commentContent',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: COMMENT_TWEET_DISCRIMINATOR })
  );
}

export function getCommentTweetInstructionDataDecoder(): Decoder<CommentTweetInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['commentContent', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCommentTweetInstructionDataCodec(): Codec<
  CommentTweetInstructionDataArgs,
  CommentTweetInstructionData
> {
  return combineCodec(
    getCommentTweetInstructionDataEncoder(),
    getCommentTweetInstructionDataDecoder()
  );
}

export type CommentTweetInput<
  TAccountCommentAuthor extends string = string,
  TAccountComment extends string = string,
  TAccountTweet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  commentAuthor: TransactionSigner<TAccountCommentAuthor>;
  comment?: Address<TAccountComment>;
  tweet: Address<TAccountTweet>;
  systemProgram?: Address<TAccountSystemProgram>;
  commentContent: CommentTweetInstructionDataArgs['commentContent'];
};

export async function getCommentTweetInstructionAsync<
  TAccountCommentAuthor extends string,
  TAccountComment extends string,
  TAccountTweet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TWITTER_PROGRAM_ADDRESS,
>(
  input: CommentTweetInput<
    TAccountCommentAuthor,
    TAccountComment,
    TAccountTweet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<CommentTweetInstruction<
  TProgramAddress,
  TAccountCommentAuthor,
  TAccountComment,
  TAccountTweet,
  TAccountSystemProgram
>> {
  // Program address.
  const programAddress = config?.programAddress ?? TWITTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    commentAuthor: { value: input.commentAuthor ?? null, isWritable: true },
    comment: { value: input.comment ?? null, isWritable: true },
    tweet: { value: input.tweet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.comment.value) {
    accounts.comment.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        seed,
        getAddressEncoder().encode(
          expectAddress(accounts.commentAuthor.value)
        ),
        new Uint8Array(await crypto.subtle.digest("SHA-256", new TextEncoder().encode(input.commentContent))),
        getAddressEncoder().encode(expectAddress(accounts.tweet.value)),
      ],
    });
  }

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.commentAuthor),
      getAccountMeta(accounts.comment),
      getAccountMeta(accounts.tweet),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getCommentTweetInstructionDataEncoder().encode(
      args as CommentTweetInstructionDataArgs
    ),
  } as CommentTweetInstruction<
    TProgramAddress,
    TAccountCommentAuthor,
    TAccountComment,
    TAccountTweet,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedCommentTweetInstruction<
  TProgram extends string = typeof TWITTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    commentAuthor: TAccountMetas[0];
    comment: TAccountMetas[1];
    tweet: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CommentTweetInstructionData;
};

export function parseCommentTweetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCommentTweetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      commentAuthor: getNextAccount(),
      comment: getNextAccount(),
      tweet: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCommentTweetInstructionDataDecoder().decode(instruction.data),
  };
}
