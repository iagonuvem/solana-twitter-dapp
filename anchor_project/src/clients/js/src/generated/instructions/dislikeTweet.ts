/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { TWITTER_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const DISLIKE_TWEET_DISCRIMINATOR = new Uint8Array([
  40, 221, 179, 49, 162, 224, 64, 97,
]);

export function getDislikeTweetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISLIKE_TWEET_DISCRIMINATOR
  );
}

export type DislikeTweetInstruction<
  TProgram extends string = typeof TWITTER_PROGRAM_ADDRESS,
  TAccountReactionAuthor extends string | AccountMeta<string> = string,
  TAccountTweetReaction extends string | AccountMeta<string> = string,
  TAccountTweet extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReactionAuthor extends string
        ? WritableSignerAccount<TAccountReactionAuthor> &
            AccountSignerMeta<TAccountReactionAuthor>
        : TAccountReactionAuthor,
      TAccountTweetReaction extends string
        ? WritableAccount<TAccountTweetReaction>
        : TAccountTweetReaction,
      TAccountTweet extends string
        ? WritableAccount<TAccountTweet>
        : TAccountTweet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DislikeTweetInstructionData = { discriminator: ReadonlyUint8Array };

export type DislikeTweetInstructionDataArgs = {};

export function getDislikeTweetInstructionDataEncoder(): FixedSizeEncoder<DislikeTweetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DISLIKE_TWEET_DISCRIMINATOR })
  );
}

export function getDislikeTweetInstructionDataDecoder(): FixedSizeDecoder<DislikeTweetInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDislikeTweetInstructionDataCodec(): FixedSizeCodec<
  DislikeTweetInstructionDataArgs,
  DislikeTweetInstructionData
> {
  return combineCodec(
    getDislikeTweetInstructionDataEncoder(),
    getDislikeTweetInstructionDataDecoder()
  );
}

export type DislikeTweetAsyncInput<
  TAccountReactionAuthor extends string = string,
  TAccountTweetReaction extends string = string,
  TAccountTweet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  reactionAuthor: TransactionSigner<TAccountReactionAuthor>;
  tweetReaction?: Address<TAccountTweetReaction>;
  tweet: Address<TAccountTweet>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getDislikeTweetInstructionAsync<
  TAccountReactionAuthor extends string,
  TAccountTweetReaction extends string,
  TAccountTweet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TWITTER_PROGRAM_ADDRESS,
>(
  input: DislikeTweetAsyncInput<
    TAccountReactionAuthor,
    TAccountTweetReaction,
    TAccountTweet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  DislikeTweetInstruction<
    TProgramAddress,
    TAccountReactionAuthor,
    TAccountTweetReaction,
    TAccountTweet,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TWITTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reactionAuthor: { value: input.reactionAuthor ?? null, isWritable: true },
    tweetReaction: { value: input.tweetReaction ?? null, isWritable: true },
    tweet: { value: input.tweet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tweetReaction.value) {
    accounts.tweetReaction.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            84, 87, 69, 69, 84, 95, 82, 69, 65, 67, 84, 73, 79, 78, 95, 83, 69,
            69, 68,
          ])
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.reactionAuthor.value)
        ),
        getAddressEncoder().encode(expectAddress(accounts.tweet.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.reactionAuthor),
      getAccountMeta(accounts.tweetReaction),
      getAccountMeta(accounts.tweet),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getDislikeTweetInstructionDataEncoder().encode({}),
  } as DislikeTweetInstruction<
    TProgramAddress,
    TAccountReactionAuthor,
    TAccountTweetReaction,
    TAccountTweet,
    TAccountSystemProgram
  >;

  return instruction;
}

export type DislikeTweetInput<
  TAccountReactionAuthor extends string = string,
  TAccountTweetReaction extends string = string,
  TAccountTweet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  reactionAuthor: TransactionSigner<TAccountReactionAuthor>;
  tweetReaction: Address<TAccountTweetReaction>;
  tweet: Address<TAccountTweet>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getDislikeTweetInstruction<
  TAccountReactionAuthor extends string,
  TAccountTweetReaction extends string,
  TAccountTweet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TWITTER_PROGRAM_ADDRESS,
>(
  input: DislikeTweetInput<
    TAccountReactionAuthor,
    TAccountTweetReaction,
    TAccountTweet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): DislikeTweetInstruction<
  TProgramAddress,
  TAccountReactionAuthor,
  TAccountTweetReaction,
  TAccountTweet,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TWITTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reactionAuthor: { value: input.reactionAuthor ?? null, isWritable: true },
    tweetReaction: { value: input.tweetReaction ?? null, isWritable: true },
    tweet: { value: input.tweet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.reactionAuthor),
      getAccountMeta(accounts.tweetReaction),
      getAccountMeta(accounts.tweet),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getDislikeTweetInstructionDataEncoder().encode({}),
  } as DislikeTweetInstruction<
    TProgramAddress,
    TAccountReactionAuthor,
    TAccountTweetReaction,
    TAccountTweet,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedDislikeTweetInstruction<
  TProgram extends string = typeof TWITTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    reactionAuthor: TAccountMetas[0];
    tweetReaction: TAccountMetas[1];
    tweet: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: DislikeTweetInstructionData;
};

export function parseDislikeTweetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDislikeTweetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      reactionAuthor: getNextAccount(),
      tweetReaction: getNextAccount(),
      tweet: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDislikeTweetInstructionDataDecoder().decode(instruction.data),
  };
}
