//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const COMMENT_REMOVE_DISCRIMINATOR: [u8; 8] = [10, 190, 215, 145, 65, 59, 112, 197];

/// Accounts.
#[derive(Debug)]
pub struct CommentRemove {
      
              
          pub comment_author: solana_pubkey::Pubkey,
          
              
          pub comment: solana_pubkey::Pubkey,
      }

impl CommentRemove {
  pub fn instruction(&self, args: CommentRemoveInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: CommentRemoveInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.comment_author,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.comment,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = borsh::to_vec(&CommentRemoveInstructionData::new()).unwrap();
          let mut args = borsh::to_vec(&args).unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::TWITTER_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct CommentRemoveInstructionData {
            discriminator: [u8; 8],
            }

impl CommentRemoveInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [10, 190, 215, 145, 65, 59, 112, 197],
                                }
  }
}

impl Default for CommentRemoveInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct CommentRemoveInstructionArgs {
                  pub comment_content: String,
      }


/// Instruction builder for `CommentRemove`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` comment_author
                ///   1. `[writable]` comment
#[derive(Clone, Debug, Default)]
pub struct CommentRemoveBuilder {
            comment_author: Option<solana_pubkey::Pubkey>,
                comment: Option<solana_pubkey::Pubkey>,
                        comment_content: Option<String>,
        __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CommentRemoveBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            #[inline(always)]
    pub fn comment_author(&mut self, comment_author: solana_pubkey::Pubkey) -> &mut Self {
                        self.comment_author = Some(comment_author);
                    self
    }
            #[inline(always)]
    pub fn comment(&mut self, comment: solana_pubkey::Pubkey) -> &mut Self {
                        self.comment = Some(comment);
                    self
    }
                    #[inline(always)]
      pub fn comment_content(&mut self, comment_content: String) -> &mut Self {
        self.comment_content = Some(comment_content);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = CommentRemove {
                              comment_author: self.comment_author.expect("comment_author is not set"),
                                        comment: self.comment.expect("comment is not set"),
                      };
          let args = CommentRemoveInstructionArgs {
                                                              comment_content: self.comment_content.clone().expect("comment_content is not set"),
                                    };
    
    accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
  }
}

  /// `comment_remove` CPI accounts.
  pub struct CommentRemoveCpiAccounts<'a, 'b> {
          
                    
              pub comment_author: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub comment: &'b solana_account_info::AccountInfo<'a>,
            }

/// `comment_remove` CPI instruction.
pub struct CommentRemoveCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
      
              
          pub comment_author: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub comment: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: CommentRemoveInstructionArgs,
  }

impl<'a, 'b> CommentRemoveCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: CommentRemoveCpiAccounts<'a, 'b>,
              args: CommentRemoveInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              comment_author: accounts.comment_author,
              comment: accounts.comment,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.comment_author.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.comment.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = borsh::to_vec(&CommentRemoveInstructionData::new()).unwrap();
          let mut args = borsh::to_vec(&self.__args).unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::TWITTER_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.comment_author.clone());
                        account_infos.push(self.comment.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `CommentRemove` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` comment_author
                ///   1. `[writable]` comment
#[derive(Clone, Debug)]
pub struct CommentRemoveCpiBuilder<'a, 'b> {
  instruction: Box<CommentRemoveCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CommentRemoveCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(CommentRemoveCpiBuilderInstruction {
      __program: program,
              comment_author: None,
              comment: None,
                                            comment_content: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      #[inline(always)]
    pub fn comment_author(&mut self, comment_author: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.comment_author = Some(comment_author);
                    self
    }
      #[inline(always)]
    pub fn comment(&mut self, comment: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.comment = Some(comment);
                    self
    }
                    #[inline(always)]
      pub fn comment_content(&mut self, comment_content: String) -> &mut Self {
        self.instruction.comment_content = Some(comment_content);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = CommentRemoveInstructionArgs {
                                                              comment_content: self.instruction.comment_content.clone().expect("comment_content is not set"),
                                    };
        let instruction = CommentRemoveCpi {
        __program: self.instruction.__program,
                  
          comment_author: self.instruction.comment_author.expect("comment_author is not set"),
                  
          comment: self.instruction.comment.expect("comment is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct CommentRemoveCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            comment_author: Option<&'b solana_account_info::AccountInfo<'a>>,
                comment: Option<&'b solana_account_info::AccountInfo<'a>>,
                        comment_content: Option<String>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

